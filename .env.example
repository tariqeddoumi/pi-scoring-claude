# PostgreSQL / Supabase
# Connexion poolée (runtime applicatif)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/pi_scoring?schema=public"
# Connexion directe (migrations Prisma)
DIRECT_URL="postgresql://postgres:postgres@localhost:5432/pi_scoring?schema=public"

# Auth (placeholder — brancher NextAuth/Supabase Auth selon déploiement)
AUTH_SECRET="change-me-in-production"

# App
NEXT_PUBLIC_APP_NAME="PI Scoring BKAM"
